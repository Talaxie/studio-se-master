<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.talend.studio</groupId>
		<artifactId>parent-pom</artifactId>
		<version>8.9.0-SNAPSHOT</version>
		<relativePath>../../talend.studio.parent.pom</relativePath>
	</parent>

	<artifactId>tos-products-parent-pom</artifactId>
	<packaging>pom</packaging>

	<repositories>
		<repository>
			<id>talend_open</id>
			<releases>
				<enabled>true</enabled>
			</releases>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
			<url>${talend.nexus.host}/repository/talaxie/</url>
		</repository>
		<repository>
			<id>talend_open_snapshots</id>
			<releases>
				<enabled>false</enabled>
			</releases>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
			<url>${talend.nexus.host}/repository/talaxie/</url>
		</repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>releases</id>
			<releases>
				<enabled>true</enabled>
			</releases>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
			<url>${talend.nexus.host}/repository/talaxie/</url>
		</pluginRepository>
		<pluginRepository>
			<id>snapshots</id>
			<releases>
				<enabled>false</enabled>
			</releases>
			<snapshots>
				<enabled>true</enabled>
			</snapshots>
			<url>${talend.nexus.host}/repository/talaxie/</url>
		</pluginRepository>
	</pluginRepositories>
	<properties>
		<talend.nexus.host>https://nexus.deilink.fr</talend.nexus.host>
	    <version.full>${product.version}.${product.timestamp}${release.suffix}</version.full>
		<product.final.name>
			${storage.prefix}-${product.timestamp}-V${product.version}${revision.filename}</product.final.name>
		<!-- Take the repository as studio dir for BOM. It has all the product
		components, but is not OS-specific. -->
		<studio.dir>${project.build.directory}/repository</studio.dir>
		<bom.out.dir>
			${project.build.directory}/products/${project.artifactId}-bom</bom.out.dir>
		<bom.final.name.prefix>
			${storage.prefix}-${product.timestamp}-V${product.version}-${revision.filename}</bom.final.name.prefix>
		<bom.final.name>${bom.final.name.prefix}-bom.xlsx</bom.final.name>
	</properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>properties-maven-plugin</artifactId>
				<executions>
					<execution>
						<!-- ID is important to override this in all.p2.product -->
            			<id>load-branding-properties</id>
						<phase>initialize</phase>
						<goals>
							<goal>read-project-properties</goal>
						</goals>
						<configuration>
							<files>
								<file>${product.branding.propertiesPath}</file>
							</files>
						</configuration>
					</execution>
				</executions>
			</plugin>
        	<plugin>
			    <groupId>org.codehaus.mojo</groupId>
			    <artifactId>build-helper-maven-plugin</artifactId>
			    <executions>
			        <execution>
			            <id>extract-product-timestamp</id>
			            <phase>initialize</phase>
			            <goals>
			                <goal>regex-property</goal>
			            </goals>
			            <configuration>
			                <name>product.timestamp</name>
			                <value>${buildQualifier}</value>
			                <regex>^([0-9_]{13}).*</regex>
			                <replacement>$1</replacement>
			                <failIfNoMatch>true</failIfNoMatch>
			            </configuration>
			        </execution>
			    </executions>
			</plugin>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-p2-repository-plugin</artifactId>
				<version>${tycho.version}</version>
				<executions>
					<execution>
						<id>default-archive-repository</id>
						<phase>none</phase>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.eclipse.tycho</groupId>
				<artifactId>tycho-p2-director-plugin</artifactId>
				<executions>
					<execution>
						<id>materialize-products</id>
						<goals>
							<goal>materialize-products</goal>
						</goals>
					</execution>
					<execution>
						<id>archive-products</id>
						<phase>package</phase>
						<goals>
							<goal>archive-products</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<formats>
						<macosx>tar.gz</macosx>
						<linux>tar.gz</linux>
						<win>zip</win>
					</formats>
					<products>
						<product>
							<!--We need to specify this product otherwise on Mac the build fails because of a .DS_Store found at the same level that the prodcut make the build fail.-->
							<id>${project.artifactId}</id>
							<archiveFileName>${product.final.name}</archiveFileName>
						</product>
					</products>
				</configuration>
			</plugin>
			<!--  tos-about-files & .eclipseproduct have been moved directly to
			org.talend.tos.feature feature, which adds it to product's root
			files.
				NOTICE.txt & license.txt are extracted from corresponding branding plugin by
			the branding feature, which adds them to product's root files. -->
			<plugin>
				<groupId>org.talend.studio</groupId>
				<artifactId>bomtool</artifactId>
				<version>1.0.0-SNAPSHOT</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>genNotice</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<studioDirectory>${studio.dir}</studioDirectory>
					<outputDirectory>${bom.out.dir}</outputDirectory>
					<productVersion>${product.version}</productVersion>
				</configuration>
			</plugin>
			<!-- .eclipseproduct is handled and published by the branding
			plugin, then copied into product root by the branding feature -->
			<!-- about.mappings files are handled directly in the branding
			plugin with correct properties as a filtered resource -->
		</plugins>
	</build>
	<profiles>
		<profile>
			<id>nonofficial</id>
			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.eclipse.tycho</groupId>
						<artifactId>tycho-packaging-plugin</artifactId>
						<version>${tycho.version}</version>
						<configuration>
							<strictBinIncludes>false</strictBinIncludes>
							<format>yyyyMMdd'_'HHmm</format>
							<archive>
								<addMavenDescriptor>false</addMavenDescriptor>
								<!-- By default, name of the current user is
								inserted into generated MANIFEST.MF files -->
								<manifestEntries>
									<Built-By />
								</manifestEntries>
							</archive>
							<timestampProvider>default</timestampProvider>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>official</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.eclipse.tycho</groupId>
						<artifactId>tycho-packaging-plugin</artifactId>
						<version>${tycho.version}</version>
						<configuration>
							<strictBinIncludes>false</strictBinIncludes>
							<format>yyyyMMdd'_'HHmm</format>
							<archive>
								<addMavenDescriptor>false</addMavenDescriptor>
								<!-- By default, name of the current user is
								inserted into generated MANIFEST.MF files -->
								<manifestEntries>
									<Built-By />
								</manifestEntries>
							</archive>
							<timestampProvider>default</timestampProvider>
						</configuration>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-antrun-plugin</artifactId>
						<version>1.7</version>
						<executions>
							<execution>
								<!-- Copy build to products directory -->
								<id>move-build-to-products</id>
								<phase>install</phase>
								<configuration>
									<target>
										<taskdef
											resource="net/sf/antcontrib/antcontrib.properties"
											classpathref="maven.plugin.classpath" />
										<property name="product.build.path"
											value="${product.build.id}/${storage.dir}/V${product.version}${revision.filename}" />
										<property name="product.dir"
											value="${storage}/${product.build.path}" />
										<mkdir dir="${product.dir}" />
										<echo>Moving product ZIP
											${project.build.directory}/products/${product.final.name}.zip
											to ${product.dir}</echo>
										<move todir="${product.dir}"
											file="${project.build.directory}/products/${product.final.name}.zip" />

										<!-- Copy bom -->
										<property name="bom.build.path"
											value="${product.build.id}/BOMS" />
										<property name="bom.dir"
											value="${storage}/${bom.build.path}" />
										<mkdir dir="${bom.dir}" />
										<echo>Copying ${bom.out.dir}/BOM.xlsx to
											${bom.dir}/${bom.final.name}</echo>
										<copy
											tofile="${bom.dir}/${bom.final.name}"
											file="${bom.out.dir}/BOM.xlsx" />
										<echo>
											Copying
											${bom.out.dir}/jarlist.json to
											${bom.dir}/${bom.final.name.prefix}-jarlist.json</echo>
										<copy
											tofile="${bom.dir}/${bom.final.name.prefix}-jarlist.json"
											file="${bom.out.dir}/jarlist.json" />
										<echo>
											Copying ${bom.out.dir}/NOTICE.txt
											to
											${bom.dir}/${bom.final.name.prefix}-NOTICE.txt</echo>
										<copy
											tofile="${bom.dir}/${bom.final.name.prefix}-NOTICE.txt"
											file="${bom.out.dir}/NOTICE.txt" />
										<echo>
											Copying ${bom.out.dir}/report.txt
											to
											${bom.dir}/${bom.final.name.prefix}-report.txt</echo>
										<copy
											tofile="${bom.dir}/${bom.final.name.prefix}-report.txt"
											file="${bom.out.dir}/report.txt" />
										<if>
											<and>
												<equals arg1="${env.HUDSON_URL}"
													arg2="https://ci-newbuild.talend.com/" />
												<equals arg1="${automaticnb}"
													arg2="1" />
											</and>
											<then>
												<echo
													file="${storage}/lastnb.txt"
													append="true">
													http://newbuild.talend.com/builds/${product.build.path}/${product.final.name}.zip${line.separator}</echo>
												<echo
													file="${storage}/lastnb.txt"
													append="true">
													http://newbuild.talend.com/builds/${bom.build.path}/${bom.final.name}${line.separator}</echo>
												<echo
													file="${storage}/lastnb.txt"
													append="true">
													http://newbuild.talend.com/builds/${bom.build.path}/${bom.final.name.prefix}-jarlist.json${line.separator}</echo>
												<echo
													file="${storage}/lastnb.txt"
													append="true">
													http://newbuild.talend.com/builds/${bom.build.path}/${bom.final.name.prefix}-NOTICE.txt${line.separator}</echo>
												<echo
													file="${storage}/lastnb.txt"
													append="true">
													http://newbuild.talend.com/builds/${bom.build.path}/${bom.final.name.prefix}-report.txt${line.separator}</echo>
											</then>
										</if>
										<exec executable="openssl"
											dir="${product.dir}"
											outputproperty="sha256">
											<arg line="dgst" />
											<arg line="-sha256" />
											<arg
												line="${product.final.name}.zip" />
										</exec>
										<echo
											file="${product.dir}/${product.final.name}.zip.sha256"
											append="false">${sha256}</echo>
									</target>
								</configuration>
								<goals>
									<goal>run</goal>
								</goals>
							</execution>
						</executions>
						<dependencies>
							<dependency>
								<groupId>ant-contrib</groupId>
								<artifactId>ant-contrib</artifactId>
								<version>1.0b3</version>
								<exclusions>
									<exclusion>
										<groupId>ant</groupId>
										<artifactId>ant</artifactId>
									</exclusion>
								</exclusions>
							</dependency>
							<dependency>
								<groupId>org.apache.ant</groupId>
								<artifactId>ant-nodeps</artifactId>
								<version>1.8.1</version>
							</dependency>
						</dependencies>
					</plugin>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-deploy-plugin</artifactId>
						<configuration>
							<skip>true</skip>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
